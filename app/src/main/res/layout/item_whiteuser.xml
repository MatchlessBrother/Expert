<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@drawable/item_whiteuser"
    android:layout_width="match_parent"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:layout_marginBottom="6dp"
    android:layout_marginTop="6dp"
    android:layout_height="50dp"
    android:paddingStart="12dp"
    android:paddingBottom="3dp"
    android:paddingEnd="12dp"
    android:paddingTop="3dp">

    <ImageView
        android:id="@+id/whiteuser_img"
        android:scaleType="fitCenter"
        tools:src="@mipmap/ic_helper"
        android:layout_height="40dp"
        android:layout_width="40dp"/>

    <TextView
        android:layout_height="wrap_content"
        android:textColor="@color/darkblue"
        android:layout_marginStart="8dp"
        android:id="@+id/whiteuser_name"
        android:layout_marginEnd="12dp"
        tools:text="Oh,My God!"
        android:ellipsize="middle"
        android:layout_width="0dp"
        android:layout_weight="1"
        android:singleLine="true"
        android:textSize="15sp"/>

    <ImageView
        android:padding="10dp"
        android:layout_width="38dp"
        android:layout_height="38dp"
        tools:src="@mipmap/ic_unseleted"
        android:id="@+id/whiteuser_select"
        android:textColor="@color/darkblue"/>
</LinearLayout>